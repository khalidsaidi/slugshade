export { slug, slugDetailed, slugAsync, createSlugger, uniqueSlug } from './slug';
export { presets } from './presets';
export type * from './types';

export { slug as slugify, slugDetailed as slugifyDetailed } from './slug';

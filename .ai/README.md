# .ai/
Internal agent workspace for plans, decisions, and build notes.

Rules:
- No secrets (no tokens, no private keys).
- Keep runtime code out of this folder.
- This directory MUST NOT ship to npm. The package uses a "files" allowlist to publish only dist + docs.
